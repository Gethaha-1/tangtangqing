# 待办与已知取舍（BACKLOG）

适用版本：**v1.4.0 · 第一阶段联网保存**。

## 1. 第二阶段优先事项

1. **完整司机流程**：车主邀请司机、司机接受邀请、加入已有 fleet、查看自己的 assignment、换车/停用分配。
2. **手机号身份**：在同一内部 user 下新增 `phone` identity，接入短信验证码、频控、CAPTCHA 和账号找回；不得迁移或重建账本。
3. **身份绑定与防误合并**：ChatGPT identity 与手机号 identity 必须经过已登录验证流程绑定，不能只因邮箱或手机号相似就合并 user。
4. **完整离线队列**：逐操作持久化、后台重试、顺序保证、跨设备离线编辑检测。
5. **冲突解决界面**：展示本机值、云端值和记录归属，让用户逐条选择；当前阶段只阻止覆盖并要求刷新。
6. **车队管理**：成员列表、owner/driver 角色调整、assignment 历史和审计记录。

## 2. 生产化前置

当前实现尚未部署。发布前另行完成：

- 创建 Sites 项目并核对 D1 binding、访问策略与 SIWC 行为；
- 在托管环境跑身份、车队隔离和跨账号测试；
- 制定 D1 migration 执行/回滚、自动备份和恢复演练；
- 增加错误监控、容量/性能监控、操作审计和告警；
- 复核隐私文案、数据导出/删除流程和适用合规要求；
- 完成 iPhone Safari、安卓 Chrome、微信内浏览器的真机回归。

这些动作需要用户明确授权；不能因为仓库已有 `.openai/hosting.json` 就自行部署。

## 3. 已知取舍与风险

1. **设备缓存不是完整离线队列**：当前已打开会话可断网暂存待同步快照；重新加载必须联网确认身份和 fleet，不承诺多次、跨设备离线编辑自动合并。
2. **批次可能部分成功**：服务端逐记录返回 applied/conflict/rejected；客户端刷新基线后可重试剩余差异，但暂时没有逐条冲突 UI。
3. **第一阶段仅 owner UI**：数据库和服务端已经预留 driver/assignment 边界，仍不能把它宣传为已开放司机功能。
4. **当前 ChatGPT provider subject**：Sites 当前转发邮箱作为身份 claim，但它只存在 identity 层。未来 provider claim 变化需要身份关联方案，不能把邮箱扩散到业务表。
5. **本地认证替身是固定 owner**：只允许 localhost，用于测试，不覆盖真实 SIWC 的托管验证。
6. **趟次编号动态变化**：补录旧账会使同车后续趟号顺延，这是预期行为，不持久化展示趟号。
7. **弹层刷新历史格**：弹层打开时刷新，第一次系统返回可能只消耗残留历史格。无害，暂不在 unload 修补。
8. **iOS 边缘手势理论双触发**：若真机出现，再给 `initEdgeSwipe` 补 `touchcancel`。

## 4. 原产品候选功能

| 项 | 状态/取舍 |
|---|---|
| 趟次目的地/线路备注 | 候选；涉及 trip 字段、D1 migration 和备份迁移 |
| 删除后 5 秒撤销 | 候选；联网后需要与版本/删除语义一起设计 |
| CSV 导出 | 候选；方便家庭成员对账 |
| 快记科目按频次排序 | 暂缓，避免打乱司机肌肉记忆 |
| “清空数据”二级页+倒计时 | 暂缓；生产化时与服务端删除/恢复策略一起设计 |
| 允许页面缩放 | 待办；需同时处理双击误触和移动布局 |

## 5. 提交惯例

- 一次提交只做一件事，提交说明写清“改了什么 + 为什么”；
- 版本号：修 bug 走 z，加兼容功能走 y，破坏性数据变更走 x 并配 migration；
- schema 改动必须提交生成 SQL、测试和文档；
- 每次正式发版打 tag，与 CHANGELOG 条目对应；
- 推送、PR 和生产部署都以用户当次明确授权为准。
