# v2.0.0 交付说明：运输收入、货物目录与高德定位

适用代码版本：v2.0.0，JSON / 云同步 schema v5。本文记录本轮现行边界；生产发布证据以 `deploy/NETLIFY-SUPABASE.md` 为准。

## 已实施

- 去程与返程清单是运输收入唯一日常写入口；收车只更新状态和到家日期，不再复制营收到旧收入行。
- 历史 `trip_income` 保持只读并继续参与统计；存在对应结构化去返程时不重复计数。普通同步拒绝新增/修改历史收入，完整 JSON 恢复仍可原样恢复。
- 去程货主/箱位分摊为可选。不分摊时整车原定运费就是去程营收；分摊时继续使用定点分尾差和逐人抹零规则。
- 同车最近 5 个不重复去程运费可一键复用；去程和返程默认使用该车最近一次货物。
- 车主可维护去程/返程货物目录；内置条目可改名、停用，也可新增。趟次保存稳定 ID 和当时名称快照，后续改名不篡改旧账展示。
- 修改入口统一显示“登记去程”。
- 点击定位不再弹应用自己的查询说明；系统位置权限仍由浏览器控制。成功自动填入市、区/县并保持静默，只有失败才提示。

## 定位链路与隐私边界

```text
用户点击 → 浏览器取得本次 GPS → 已登录同源 API
       → 高德坐标转换 → 高德逆地理编码 → 返回市、区/县
```

- 高德 Web 服务 Key 仅配置在 Netlify Functions，不进入 HTML、浏览器 JavaScript、仓库或日志。
- API 要求已登录会话、同源 POST、JSON 请求，限制正文和请求频率，并设置 8 秒超时。
- 发给地址服务商的只有本次坐标和协议所需参数，不附带账号、账目、车辆或历史地点；不持续定位。
- 服务商由部署变量显式选择。高德失败时不自动把同一坐标发送给备用服务商。
- 自动化只使用协议夹具；真实国内网络、系统权限和行政区覆盖以用户设备点击验收为准。

## 数据与部署

- schema v5 在 v4 上增加去/返程货物目录与趟次货物快照；v1/v2/v3/v4 可迁移到 v5。
- 继续使用 PostgreSQL 既有 `fleet_settings.business_json` 与 `trips.business_json`，不增加 migration、不清空或重写历史数据。
- 生产新增 `TTQ_LOCATION_PROVIDER=amap`、`TTQ_AMAP_WEB_SERVICE_KEY`；仅当高德控制台启用数字签名时再配置私钥。
- 客户端、同步 schema gate 和定位 API 必须同次部署，避免新旧客户端混写。

## 验收重点

- 新建及编辑去程均显示“登记去程”；无货主仍可按整车运费保存。
- 刷新后最近运费、最近货物、货物名称快照及历史收入统计保持一致。
- 定位成功后市、区/县确实写入表单，而不是只显示“已定位”；成功无应用 toast，失败才提醒且仍允许手填。
- 未登录、跨源、非法坐标、超频和未配置 Key 均 fail closed；客户端资源中搜索不到高德 Key。
- 发布只涉及 `develop`，不合并或修改 `main`。
