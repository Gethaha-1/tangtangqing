# 第一轮：草稿恢复与大型备份恢复

开发版本：1.7.0。范围确认：2026-09-06。当前为本地实现/验收，不代表已发布生产。

## 已实现的产品行为

- 快记清单和正在输入的表达式、油费字段、日期、备注按已核验账号/车队存入 IndexedDB。首页提供继续填写、丢弃普通草稿、导出核对内容。
- 本机事务成功后才显示“已暂存”；失败显式提示且不发送缺少持久请求日志的业务请求。页面内暂存退出会等待本机保存完成。
- 普通提交先保存稳定 ID 与完整请求；快记请求与草稿在同一存储记录中。未知回执刷新后先查原 ID/hash，再回读或重试原请求，不创建新 ID 盲目重记。
- 会话锁定清内存、隐藏 DOM、阻止迟到回执，但不删除本机草稿。重新登录同账号才能展示；不存在跨设备恢复保证。多标签同一草稿使用 revision compare-and-swap。
- 大型恢复仍经过原 v1/v2/v3 迁移、范围检查、摘要确认和金额/fuel 守恒；临时分块绝不提前改变正式账本，完成回读后才显示成功。
- 上传中断保留本机任务与服务器块。重新进入后可继续；若本机文件丢失，可重新选择原备份，必须与原 manifest/baseVersion 一致才能续传。取消只删除未完成的临时内容，不回滚已确认的正式恢复。
- 整车队 revision 在数据库内随业务写变化；恢复预览后其他设备新增记录，也会使最终恢复冲突，原数据保持不变。
- “保存全部”只保存清单；若输入区还有未加入的金额/备注/油费，会要求先加入或清空，不能静默丢弃。
- 若服务器已完成恢复，但其他设备后续记账使回读摘要不同，不能再次覆盖。可先导出核对内容，再明确选择“核对已完成结果”，服务器再次确认 complete 后接受当前云端账本、结束本机待核对；未完成任务不能走此入口。

## 容量与执行边界

- 输入文件与规范化变更分别限制 20 MiB（20 × 1024 × 1024 字节）；最多 30000 个变更、256 块，每块 128 KiB/250 条。替换包含删除旧账的操作，不等于文件中的业务记录数。
- 普通记账单次仍最多 500 条。大型恢复用专用临时 API，不放宽公共 sync 请求上限。
- 本轮复用现有 Next.js 路由，声明 60 秒执行窗口；HTTP 等待 45 秒，超时先核对任务。没有增加付费服务、Netlify Background Function 或无限后台运行承诺。
- 关闭页面会暂停后续分块上传；最终服务器请求可能继续，也可能被平台终止。数据库事务回滚/完整提交由服务器决定，客户端下次检查原任务，不凭断网推断失败。
- 未完成临时区 7 天有效，一车队一个活动任务；访问时清理过期未完成/取消任务。成功时删除分块，保留完成任务 manifest/身份及 `sync_commits` 幂等元信息，避免过期后把原请求当新恢复。长期无人访问时不会由本应用定时清理，需要运维审核清理；不得删除完成回执。
- 草稿是本机暂存，不是备份；清浏览器数据、设备丢失、系统存储回收仍可能造成丢失。草稿核对导出不是可直接完整恢复的正式 JSON 备份。

## 文件与迁移

- `src/draft-store.js`：IndexedDB 与 scope/CAS；`src/recovery-client.js`：分块、SHA-256、续传。
- `legacy/ledger.html`：草稿提示/恢复入口、请求日志、回执核对、导入接线；原业务算法未改。
- `app/api/restore/route.ts`、`app/api/sync/status/route.ts`、`lib/server/recovery-route.ts`、`lib/server/restore-repository.ts`：受保护恢复接口。
- `lib/server/sync-repository.ts`、`lib/server/bootstrap.ts`：集合读取、整车队版本快照、最终原子守卫与任务确认。
- `supabase/migrations/20260905164507_ledger_recovery.sql`：独立增量迁移，数据库内部 schema marker 2；JSON 仍是 schema 3。
- `db/recovery-schema.ts`、`db/runtime-schema.ts`、`db/postgres.ts`：本地兼容结构与生产 schema 校验。
- `db/schema.ts`、`drizzle/0003_tranquil_lockjaw.sql` 及生成快照：本地 SQLite 镜像；`tests/recovery-schema.test.mjs` 验证 runtime/migration 一致、旧数据守恒和 revision 回滚。不是生产 PostgreSQL 迁移。
- `tests/draft-recovery.test.mjs`、`tests/restore-integration.test.mjs`、`tests/browser/recovery.spec.mjs`：存储、真实 PostgreSQL、真实浏览器回归。
- 测试新增固定版本 `@playwright/test` 开发依赖；无新增生产依赖。使用已安装 Chrome，测试数据库目录临时创建并在进程退出时清理。

## 验收

自动化须覆盖：旧 v1/v2/v3 超过 500 条、分块重复/篡改/缺失、完整事务失败回滚、跨账号/driver 拒绝、预览后新增账目、任务取消和过期、一万条混合费用的整数金额/fuel 回读。

浏览器须覆盖：清单和未加入输入刷新恢复、服务器已保存但响应丢失后不重复入账、大型备份中断后刷新续传、多标签草稿冲突。手机 Safari/微信等真机、生产网络时限和容量不由本机测试替代。

2026-09-06 本机验收：单元/真实 PostgreSQL/SQLite 回归、真实 Chrome 移动视口回归、生产模式 HTTP 冒烟、lint、类型检查、Next.js 与 Netlify 离线产物构建均需退出码 0；测试项数以当次命令为准。万条合成费用夹具约 2.43 MB 变更数据，已验证记录、整数金额与 fuel 守恒；此结果不等于 20 MiB 上限或生产网络已验收。恢复入口截图已检查移动视口无横向溢出。

## 第二轮：明确未实施

应收／实收、客户、分次收款、固定费用、利润/实际收支新口径以及 schema v4 均待项目所有者进一步确认。本轮没有新增这些表、字段或页面。
