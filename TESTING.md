# 测试（TESTING）

适用版本：**v1.4.0 · 第一阶段联网保存**。验证分为自动化测试、构建、登录与联网冒烟、原业务冒烟、真实备份守恒五部分。

## 1. 本地准备与自动化测试

需要 Node.js 22.13 或更高版本。

```bash
npm install
npm test
npm run build
```

`npm test` 应覆盖：

| 测试文件 | 覆盖范围 |
|---|---|
| `tests/domain.test.js` | v1→v2、到家日期趟号、车辆/账期统计 |
| `tests/cloud-sync.test.js` | 独立记录拆分重组、差异计划、显式删除、迁移守恒、三方合并、并发远端新增保护、设备缓存失败 |
| `tests/legacy-xss.test.mjs` | 恶意 schema v2 备份的文本、属性和科目图标持久化 XSS 回归 |
| `tests/server-api.test.mjs` | 可信身份头、owner/driver 边界、越权 conflict、版本冲突、正式 D1 migration/CHECK、跨记录原子触发器 |

任何测试失败都不能用手工冒烟代替。D1 schema 改动还必须执行并检查：

```bash
npm run db:generate
```

生成 SQL 应保存在 `drizzle/`。检查每个 migration 只有预期表、索引、外键和约束变化。

## 2. 本地登录与路由保护

启动：

```bash
npm run dev
```

打开终端打印的 Local URL，不要猜端口。

1. 未登录打开 `/`：显示「使用 ChatGPT 登录」，并显示带「仅本地测试」标记的本地入口。
2. 未登录直接打开 `/ledger`：回到登录页，账本内容不可见。
3. 点「用本地测试账号进入」：无需密码，以 `local-owner@ttq.test` 进入 `/ledger`。
4. 返回 `/`：显示已登录身份、「继续到账本」和「退出这个账号」。
5. 点退出：本地 HttpOnly 测试 cookie 被清除，再打开 `/ledger` 仍会被拦截。
6. 把主机换成非 `localhost` / `127.0.0.1` / `[::1]`：本地登录入口不显示，直接请求 `/api/local-auth/signin` 返回 404。
7. 未带任何可信身份请求 `/api/bootstrap` 或 `/api/sync`：返回 401。

本地身份是 Worker 提供的固定替身，只验证本地流程；Sites 正式 SIWC 必须等用户决定发布后再在托管环境验证。

## 3. 首次迁移冒烟

每个分支使用独立的测试浏览器资料和本地测试 D1，绝不拿唯一真实备份直接试。

### A. 本机有 v2 旧账、云端为空

1. 在同一 Local URL 的 localStorage 中准备一份 `tangtangqing-data` v2 测试账本。
2. 登录首次进入 `/ledger`。
3. 页面必须显示本机车辆、趟次、收入、支出、维修摘要，不自动上传。
4. 先点「导出本机备份」：下载 JSON，迁移选择仍留在页面。
5. 点「核对并上传到云端」：完成后显示云端成功。
6. 刷新并重新执行：不产生重复趟次或金额；旧 localStorage 仍存在。

另起一份环境，选择「先建一个新的云端账本」：云端建立新账，本机旧账不被删除或冒充已上传。

### B. 本机和云端都有数据

1. 先让云端存在至少一条业务记录。
2. 在同源 localStorage 保留另一份含业务记录的 v2 旧账。
3. 重新打开账本：必须显示双方都有数据的警告，不自动合并或覆盖。
4. 导出本机 JSON 后选择进入云端：云端数据保持不变，本机旧账仍保留。

### C. 本机没有旧账

1. 云端已有账：进入后直接显示该车队数据。
2. 云端为空：自动建立默认车队账本。
3. 两种情况都不能把设备空缓存当成云端数据。

### 守恒检查

迁移前后核对：

- 车辆、收入/支出科目、趟次、趟次收入、趟次支出、维修数量；
- 趟次收入、趟次支出、维修金额总和；
- 所有趟次和维修是否有车辆归属；
- 再次上传是否无新增差异。

## 4. 保存失败、断网与并发

1. 正常改一笔：状态依次显示「正在保存」和「已保存到云端」。
2. 成功打开一次后切断网络，再改一笔：必须显示「尚未上传 · 已暂存在此设备」，不能显示云端成功。
3. 刷新断网页面：应阻止业务缓存冒充已重新鉴权的云端账本；恢复网络后才能按当前 fleet 载入并续传。
4. 恢复网络并点重试：成功后显示已补传；失败则继续保留未上传提示。
5. 用两个独立浏览器上下文打开同一车队，基于相同 version 修改同一记录：先保存者成功，后保存者看到「云端有新改动」，不能覆盖。
6. 两端分别修改不同车辆或不同收入/支出记录：只提交各自变化，不覆盖无关记录。
7. 一端离线待传、另一端新增不同记录：恢复后做三方合并，远端新增记录仍存在且不会生成 `delete`。
8. 禁用 localStorage 或填满浏览器配额后再让云端保存失败：页面必须提示「设备缓存也失败，请立即导出」，不能显示已暂存。
9. 尝试在同步 body 中自造 `fleetId`、`userId`、role、membership、identity 或 assignment：服务端拒绝。
10. owner 可读写车队所有车辆；为 driver 准备测试 membership 时，无活动 assignment 的车辆必须不可见、不可写。
11. 交错停用最后两辆车、停用与发车、删除趟次与新增子账目：数据库原子约束必须拒绝破坏业务规则的一方。

## 5. 浏览器内置自检

登录后打开：

```text
/ledger#test
```

应弹出 28 项自检报告，也可在控制台执行 `runSelfTests()`。覆盖金额表达式、XSS 转义、schema v1→v2、账期、趟号、车辆隔离、统计和维修口径。

## 6. 原业务手工冒烟

手机浏览器优先：

1. 添加第二辆车；两辆车分别发车；同一辆车重复发车被拦截。
2. 快记输入 `300+250`；改成昨天；快速连点保存仍只入账一笔。
3. 零收入收车先警告；填收入后滑动收车，出现收讫章。
4. 修改/删除支出和收入，补录旧账；同车趟号顺延，另一车不变。
5. 新建、改名、停用科目；历史记录仍显示。
6. 设置跨自然年或非 1 月 1 日账期；首页、列表、统计、维修和报告一致。
7. 系统返回键逐层关弹层；第三次才离开页面。
8. 主题切换后刷新仍正确；支出超过收入时百分比显示真实值。
9. 导出 JSON；明确恢复后云端保存成功；断网恢复时只显示设备暂存。
10. v1 备份恢复后出现「原有车辆」，旧趟次、维修、金额和科目完整。

涉及手势、History、剪贴板或分享时，至少验证 iPhone Safari、安卓 Chrome 和微信内置浏览器。微信内 Web Share 不可用时应降级为复制。

## 7. 真实备份只读守恒

```bash
node scripts/verify-backup.js /绝对路径/趟趟清备份.json
```

脚本只读输入并输出数量/金额摘要。不得把真实账本复制进仓库、写回原文件或上传到第三方。

## 8. 本地交付清单

- [ ] `npm test` 全绿
- [ ] `npm run build` 成功
- [ ] `/ledger#test` 28 项全绿
- [ ] 登录、退出和未授权路由验证通过
- [ ] 三个首次迁移分支验证通过
- [ ] 断网、重试、同记录冲突和不同记录并发验证通过
- [ ] 原业务冒烟通过
- [ ] 真实备份守恒校验通过
- [ ] schema 与 `drizzle/` migration 已检查
- [ ] `APP_VER`、CHANGELOG 和架构文档一致
- [ ] 未推送 GitHub、未创建 PR、未部署 Sites 生产
