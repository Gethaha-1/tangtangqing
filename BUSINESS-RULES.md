# 业务规则（BUSINESS-RULES）

适用版本：v1.5.0 + Unreleased 本地改进。这里是趟号、账期、车辆、油费与统计口径的权威说明。

## 1. 账期与报告范围

- 用户在「更多 → 账期设置」中设置开始日期和结束日期；账期含首尾两日，最长 366 天；
- 首页、趟次列表、统计、维修保养和账期报告使用同一个账期和车辆筛选；
- 月报读取所选自然月与当前账期的交集；没有交集时不生成报告；
- 年报读取所选完整自然年，不受当前账期截断；
- 已收车趟次按 `endDate` 归期，油费沿用所属已收车趟次的 `endDate`；维修按自身 `date` 归期；
- 在途趟始终允许在首页和趟次页看到，避免切换账期后无法收车。

## 2. 趟次排序和编号

- 已收车趟次以 `endDate`（到家日期）作为业务排序日期，在途趟暂以 `startDate` 排序；
- 同一业务日期依次以 `closedAt`、`createdAt`、`id` 打破并列；列表按业务日期倒序展示；
- 趟号按「车辆 + 当前账期」独立计算，按业务日期正序从 1 开始；
- 趟号是动态展示值，不存入数据。补录更早到家的旧账会插入正确位置，并使后续趟号顺延；
- 跨账期趟按到家日期归入账期。在途阶段显示预计趟号，收车后才确定。

对应实现：`src/domain.js` 中的 `tripDate`、`compareTripsAsc/Desc`、`tripSeq`。

## 3. 多车辆

- `vehicles` 保存车辆目录；车辆名称必填，车牌可选；
- 每条趟次和维修记录都必须有 `vehicleId`；
- 每辆车最多一趟在途，不同车辆可以同时在途；
- 单车范围只读取该车的趟次、收入、支出、油费和维修；「全部车辆」聚合当前范围内全部车辆，但各车趟号仍独立；
- 有历史记录的车辆采用停用，不物理删除；在途车辆不能停用，且始终至少保留一辆启用车辆。

这些跨记录约束必须由数据库原子约束兜底，不能只依赖客户端提示或服务端“先查后写”。

## 4. 金额、维修与净利润

- 单趟利润 = 该趟收入合计 − 该趟支出合计；维修绝不硬分摊到某一趟；
- 账期趟次利润 = 同一账期、同一车辆范围内全部已收车趟次的收入 − 趟内支出；
- 维修保养按自身 `date`、`vehicleId` 以简单现金制计入发生账期，单独列账；不引入折旧或摊销；
- 本账期净利润 = 账期趟次利润 − 同一账期、同一车辆范围内的维修保养；
- 首页、统计和账期/月度/年度报告都展示趟次利润、维修保养和净利润，不能把“趟次利润”误标为“净利润”；
- 普通金额沿用两位小数、非负和既有限额规则。

## 5. 油费三项与统计

- 油费仍是所属趟次中的 `fuel` 支出科目，但新日常记录保存结构化 `fuel` 元数据，不把单价、升数塞进备注；普通支出不出现这些字段；
- 三项为总价（元）、油品单价（元/升）、加油升数（升）。任填两项自动计算第三项；三项都填时必须一致，否则不能保存；
- 总价最多 2 位小数，单价最多 4 位，升数最多 3 位；三项一致性允许固定 0.01 元误差，不接受指数形式、零、负数或越界值；
- D1 分别保存整数分、`fuel_unit_price_x10000` 和 `fuel_volume_ml`，服务端再次执行同一精度与一致性校验；
- 历史油费可能只有总价。显式补录旧账及编辑旧记录可保留“仅金额”兼容路径，并标记为旧油费；不得为它伪造升数或油价；
- 周期/月度/年度摘要包括总油费、结构化总升数、按升数加权均价、最低/最高记录油价、轻量油价趋势及分车辆油费/升数；旧油费金额计入总油费，但不进入升数、均价和油价极值，并显示覆盖缺口；
- 百公里油耗、每趟油耗效率需要里程或起止里程表，当前数据不足，延期决定且不得推算伪数据。

## 6. 收车确认

- 首页及趟次详情使用拇指易触达的全宽“确认收车”按钮，不再使用长距离收车控件；
- 第一次点击进入收车结算，再点击后打开真实的嵌套确认 sheet，展示车辆、到家日期、收入、支出和预计利润；
- 零收入时明确警告，但用户仍可确认；“返回检查”、系统返回和弹层历史栈逐层返回；
- 最终提交期间按钮锁定并显示保存状态。只有服务器完整确认后才关闭全部相关 sheet 并显示成功；失败、409 或 422 均保留确认层和表单内容。

### 快记清单

- 「记一笔」中的普通费用与结构化油费先加入当前页面的待保存清单；加入时生成稳定记录 ID，可在清单中修改或移除；
- 清单只属于同一趟次，显示笔数和整数分合计。它不是正式账本，也不是离线队列，不写入 localStorage/sessionStorage；刷新、退出登录、401、换账号或 BFCache 锁定都会清除；
- 用户点击「保存全部 N 笔」才构成一次业务动作：全部条目通过一个 `operationId` 绑定的原子请求提交，全部确认后才进入正式状态；不得逐条上传或拆批；
- 400/422 保留可修改清单；请求已发出但回执未知时，清单与操作 ID 一起进入待核对状态并锁定修改，重连仅可重放完全相同的请求；409 不覆盖云端；
- 未提交清单退出前必须明确提醒；已经发出、等待核对的清单不能在页面内假装丢弃。

## 7. 报告契约与延期边界

- `src/domain.js` 的 `buildReportSummary()` 是页面卡片、文字报告和未来渲染流程共同使用的纯结构化摘要契约；
- 输入仅包含账期/月份/年份与车辆范围，输出统一财务、油费、范围和覆盖警告；
- 当前没有定期调度、视频生成、外部 OpenAI/API 调用、视频依赖或报告网络 endpoint；未来方案必须另行评估服务、费用、隐私、权限和用户审核流程。

## 8. 严格在线与当前认证边界

- 当前实现的正式业务数据源是服务端 D1；业务动作只有在服务器原子确认整批后才进入正式状态，失败提案不自动上传、不显示成功；
- 每批使用完整 `expectedVersion` 和稳定 `operationId`；收车+收入、删除趟次+子账等必须全成或全不成；
- 未真实连接服务器时，已加载账本只读；恢复网络后必须重新 POST bootstrap 身份、fleet、权限和版本；
- 数据归属于 `fleet`，不直接归属于邮箱或手机号。内部角色预留 `owner` / `driver`，所有身份、归属、角色和车辆分配检查在服务端完成；
- 应用边界使用 provider-neutral `Principal`。当前 `sites` 与严格 loopback `local` 适配器可用；`cloudbase` 只是拒绝式占位，尚未实现运行时认证；
- 本地测试账号以纯数字手机号 `13800000000` 作为显示登录名，无密码且仅限显式 development loopback；
- 账号相关浏览器状态必须在 bootstrap 返回 `fleet.id + membership.id` 后按 scope 存储；未归属的旧 `tangtangqing-data` 等 localStorage 不自动读取或迁移；
- 离线写入、自动离线合并和逐条冲突解决不属于当前能力。
