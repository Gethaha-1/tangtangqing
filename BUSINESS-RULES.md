# 业务规则（BUSINESS-RULES）

适用版本：v2.0.1。这里是趟次、去返程运费、账期、车辆、油费与统计口径的权威说明。

## 1. 账期与报告范围

- 用户在「更多 → 账期设置」中设置开始日期和结束日期；账期含首尾两日，最长 366 天；
- 首页、趟次列表、统计、维修保养和账期报告使用同一个账期和车辆筛选；
- 月报读取所选自然月与当前账期的交集；没有交集时不生成报告；
- 年报读取所选完整自然年，不受当前账期截断；
- 已收车趟次按 `endDate` 归期，油费沿用所属已收车趟次的 `endDate`；维修按自身 `date` 归期；
- 在途趟始终允许在首页和趟次页看到，避免切换账期后无法收车。

## 2. 趟次排序和编号

- 已收车趟次以 `endDate`（到家日期）作为业务排序日期，在途趟暂以 `startDate` 排序；
- 同一业务日期依次以 `closedAt`、`createdAt`、`id` 打破并列；列表按业务日期倒序展示；
- 趟号按「车辆 + 当前账期」独立计算，按业务日期正序从 1 开始；
- 趟号是动态展示值，不存入数据。补录更早到家的旧账会插入正确位置，并使后续趟号顺延；
- 跨账期趟按到家日期归入账期。在途阶段显示预计趟号，收车后才确定。

对应实现：`src/domain.js` 中的 `tripDate`、`compareTripsAsc/Desc`、`tripSeq`。

## 3. 多车辆

- `vehicles` 保存车辆目录；车辆名称必填，车牌可选；
- 每条趟次和维修记录都必须有 `vehicleId`；
- 每辆车最多一趟在途，不同车辆可以同时在途；
- 单车范围只读取该车的趟次、收入、支出、油费和维修；「全部车辆」聚合当前范围内全部车辆，但各车趟号仍独立；
- 有历史记录的车辆采用停用，不物理删除；在途车辆不能停用，且始终至少保留一辆启用车辆。

这些跨记录约束必须由数据库原子约束兜底，不能只依赖客户端提示或服务端“先查后写”。

## 4. 金额、维修与净利润

- 单趟利润 = 该趟收入合计 − 该趟支出合计；维修绝不硬分摊到某一趟；
- 账期趟次利润 = 同一账期、同一车辆范围内全部已收车趟次的收入 − 趟内支出；
- 维修保养按自身 `date`、`vehicleId` 以简单现金制计入发生账期，单独列账；不引入折旧或摊销；
- 本账期净利润 = 账期趟次利润 − 同一账期、同一车辆范围内的维修保养；
- 首页、统计和账期/月度/年度报告都展示趟次利润、维修保养和净利润，不能把“趟次利润”误标为“净利润”；
- 普通金额沿用两位小数、非负和既有限额规则。
- 新业务数字字段使用网页大键盘，普通十进制：`240` 就是 240，不做“补两位”猜测；吨位、箱位和掉称最多 3 位小数，金额最多 2 位。支持清空、零值、退格；取消键盘不修改字段。
- 新业务公式在客户端预览，服务端用定点整数重算；不信任客户端传入的汇总值。

## 5. 一趟往返与发车清单

- 一条 `trip` 代表从发车到收车的整个往返；去程与返程都保存在同一趟的 `business` 中。
- 去程和返程货物目录由车主在“运输业务设置”中维护；内置去程为拉菜、普货、其他，内置返程为玉米、玉米片、大豆、稻谷、普货、其他。目录项使用稳定 ID，可改名或停用；旧趟次继续显示保存时的名称快照。
- 登记去程默认复用该车最近一次货物，且可不选货物。货主可有多个常用市场，分组保存多个“货主 + 市场”成员。
- 分组的主货主只是快捷入口；点它会把整组加入本趟。本趟再点任意成员圆圈（包括主货主）只移除那一人，不改设置中的分组。
- `+货主` 每次只加一个货主卸货点。临时新建的货主/市场与本趟清单在最终按钮中一次原子保存。

## 6. 去程整车运费分摊

- 整车原定运费可手动修改；货主分摊是可选项。没有分摊明细时，整车原定运费直接作为去程营收。
- 分摊额 = 本人箱位 ÷ 总箱位 × 整车原定运费。先为每人取整数分，剩余分按余数从大到小分配；余数相同时按清单顺序，保证结果确定且分摊合计等于整车原定运费。
- 协商抹零按货主单独记录，最终确认额 = 分摊额 − 抹零。抹零不生成支出、收据或债务，也不与分尾差混同。
- 有分摊时，趟次去程营收取所有最终确认额合计；无分摊时取整车原定运费。兼容的 `cargo` 收入行不再重复累加。

## 7. 返程、应收/实收与称重

- 返程货物从车主维护的返程目录选择，默认复用该车最近一次货物；计费、称重、扣款和地点在同一页。
- 返程应收 = 装车吨位 × 单价，四舍五入到分。实收未填（`null`）时首页营收取应收；实收一旦填写就优先，数字 `0` 表示明确实收为零。清空实收才回退应收。
- 填写吨位、单价、实收中的两项时，自动补齐空缺项；吨位保留三位，单价和实收保留两位。来源变化只联动自动项；三项均手工填写或从正式账本重新打开时保留已确认差额，避免改写真实称重或实收 0。需重新反算时可清空目标项，再修改来源。自动项来源在本机草稿恢复时保留，来源清空/非法时清空过期自动值，不用零金额反推出零吨位或零单价。
- 返程营收只计一次；兼容的 `back` 收入行不与结构化返程重复累加。
- 装卸位置各有“清除坐标”（保留文字）和“清空位置”（清除本次整段位置）；两者都会取消在途定位。带有常用地点引用的位置清除坐标后保存，会同步移除该常用地点的旧坐标；清空位置只解除当前趟次引用，不删除常用地点或其他历史趟次。
- 卸车吨位、人工确认掉称和协商掉称扣款均可选。装/卸车吨位齐全时只推导公斤参考；未授权任何“每公斤扣多少钱”公式，协商扣款也不从已确认营收再减一次。
- 卸车吨位高于装车吨位时必须人工勾选确认，否则客户端不加入清单、服务端也拒绝。
- 装/卸车的市、区县分开，厂家/地点、道路、装卸和其他提醒均可选，可重用常用地点。旧 `region` 原文保留，不猜测拆分；新 `city/county` 随业务 JSON 同步和备份。
- 点击定位后不再弹出应用自己的查询确认；浏览器原生位置权限仍需用户授权。浏览器只把本次实时 GPS 坐标发给已登录的同源 `/api/location/reverse`，服务端以高德 Web 服务先做 WGS84→GCJ02 转换，再逆地理编码并返回市、区县；高德 Key 只存在服务端。请求不附带账号、账目或历史坐标，不持续定位。8 秒查询超时或行政区缺失时提示错误并允许手填；成功保持静默。结果晚到不能覆盖查询期间手填的市县或已切换的常用地点，退出/放弃后丢弃结果。服务商只能由部署配置显式选择，失败时不会把同一坐标自动转发给另一家。

## 8. 运输收入单一写入口与历史兼容

- 新版运输收入只通过同一趟的去程、返程业务清单登记；普通同步拒绝新增或修改旧 `trip_income` 行。
- 历史收入保持只读并继续参与统计。存在结构化去程/返程时，对应的旧 `cargo`/`back` 兼容行不再重复计入；其他历史手工收入仍计入。
- JSON 完整恢复仍能原样恢复历史收入；删除旧收入保留为显式维护能力。收车只改变趟次状态和日期，不创建或重写运输收入。

## 9. 油费三项与统计

- 油费仍是所属趟次中的 `fuel` 支出科目，但新日常记录保存结构化 `fuel` 元数据，不把单价、升数塞进备注；普通支出不出现这些字段；
- 三项为总价（元）、油品单价（元/升）、加油升数（升）。任填两项自动计算第三项；三项都填时必须一致，否则不能保存；
- 总价最多 2 位小数，单价最多 4 位，升数最多 3 位；三项一致性允许固定 0.01 元误差，不接受指数形式、零、负数或越界值；
- PostgreSQL 分别保存整数分、`fuel_unit_price_x10000` 和 `fuel_volume_ml`，服务端再次执行同一精度与一致性校验；本地 SQLite 兼容层使用相同字段语义；
- 历史油费可能只有总价。显式补录旧账及编辑旧记录可保留“仅金额”兼容路径，并标记为旧油费；不得为它伪造升数或油价；
- 周期/月度/年度摘要包括总油费、结构化总升数、按升数加权均价、最低/最高记录油价、轻量油价趋势及分车辆油费/升数；旧油费金额计入总油费，但不进入升数、均价和油价极值，并显示覆盖缺口；
- 百公里油耗、每趟油耗效率需要里程或起止里程表，当前数据不足，延期决定且不得推算伪数据。

## 10. 收车确认

- 首页及趟次详情使用拇指易触达的全宽“确认收车”按钮，不再使用长距离收车控件；
- 第一次点击进入收车结算，再点击后打开真实的嵌套确认 sheet，展示车辆、到家日期、收入、支出和预计利润；
- 零收入时明确警告，但用户仍可确认；“返回检查”、系统返回和弹层历史栈逐层返回；
- 最终提交期间按钮锁定并显示保存状态。只有服务器完整确认后才关闭全部相关 sheet 并显示成功；失败、409 或 422 均保留确认层和表单内容。

### 快记清单

- 「记一笔」中的普通费用与结构化油费先加入当前页面的待保存清单；加入时生成稳定记录 ID，可在清单中修改或移除；
- 清单只属于同一趟次，显示笔数和整数分合计。它不是正式账本，也不是离线队列；清单及尚未加入的输入可按已核验 fleet+membership 暂存在 IndexedDB。刷新可恢复，退出/401/BFCache 清内存但保留本机暂存，换账号不可见；
- 点击「保存全部 N 笔」或清单页返回构成一次业务动作：已加入的条目通过一个 `operationId` 绑定的原子请求提交，全部确认后才进入正式状态；不得逐条上传或拆批。返回先可靠暂存请求就关闭，不等云端回执。未加入的输入/正在编辑但未确认的条目保留本机；
- 400/422 保留可修改清单；请求已发出但回执未知时，清单与操作 ID 一起进入待核对状态并锁定修改，重连仅可重放完全相同的请求；409 不覆盖云端；
- 底部“放弃修改”不上传，等待已排队本机写入后删除草稿；已发出的请求不能假装撤销。未填写或改回原样的业务表单退出不弹保存确认。

本机草稿保存失败必须明确提示，不能声称已暂存，也不发送缺少可靠请求日志的业务写入；用户仍可明确放弃。原趟次删除、换车或科目停用时不擅自改归属。发车返回只上传完整有效的清单；返程返回只上传最后一次“加入本次清单”的快照，不完整或未加入的输入仅保留本机。逐字段输入不逐键上传。上传中可浏览，其他写入仍按原顺序等待，不能把“关闭了页面”当作“云端已保存”。

去返程的“加入清单”均为可选预览；底部保存按钮校验并原子提交当前最新填写内容，不要求先加入/重新加入。每趟仍是一份去程和一份返程，重复加入只更新预览，不新增运输收入。保存防重复由当前提交状态及幂等请求保障，不使用跨两次编辑的时间锁静默吞掉保存。

## 10. 报告契约与延期边界

- `src/domain.js` 的 `buildReportSummary()` 是页面卡片、文字报告和未来渲染流程共同使用的纯结构化摘要契约；
- 输入仅包含账期/月份/年份与车辆范围，输出统一财务、油费、范围和覆盖警告；
- 当前没有定期调度、视频生成、外部 OpenAI/API 调用、视频依赖或报告网络 endpoint；未来方案必须另行评估服务、费用、隐私、权限和用户审核流程。

## 11. 严格在线与当前认证边界

- 当前线上正式业务数据源是 Supabase PostgreSQL 的 `ttq` 私有 schema；业务动作只有在服务器原子确认整批后才进入正式状态，失败提案不自动上传、不显示成功；
- 每批使用完整 `expectedVersion` 和稳定 `operationId`；收车+收入、删除趟次+子账等必须全成或全不成；
- 未真实连接服务器时，已加载账本只读；恢复网络后必须重新 POST bootstrap 身份、fleet、权限和版本；
- 数据归属于 `fleet`，不直接归属于邮箱或手机号。内部角色预留 `owner` / `driver`，所有身份、归属、角色和车辆分配检查在服务端完成；
- 应用边界使用 provider-neutral `Principal`。线上只使用 Supabase Auth；严格 loopback `local` 适配器只用于开发测试，任何生产环境不得启用；
- 本地测试账号以纯数字手机号 `13800000000` 作为显示登录名，无密码且仅限显式 development loopback；
- 账号相关浏览器状态必须在 bootstrap 返回 `fleet.id + membership.id` 后按 scope 存储；未归属的旧 `tangtangqing-data` 等 localStorage 不自动读取或迁移；
- 离线写入、自动离线合并和逐条冲突解决不属于当前能力。
