# 业务规则（BUSINESS-RULES）

适用版本：v1.5.0。这里是趟号、账期、车辆和统计口径的唯一权威说明。严格在线保存不改变以下业务口径。

## 1. 账期

- 用户在「更多 → 账期设置」中设置开始日期和结束日期；
- 账期含开始日和结束日，当前版本最长 366 天；
- 首页、趟次列表、统计、维修保养和账期报告使用同一个账期；
- 月报仍按月份生成，但只读取该月份与当前账期重合的记录；
- 在途趟始终允许在首页和趟次页看到，避免账期切换后无法收车。

## 2. 趟次排序和编号

- 已收车趟次以 `endDate`（到家日期）作为业务排序日期；
- 在途趟暂以 `startDate` 作为排序日期；
- 同一业务日期依次以 `closedAt`、`createdAt`、`id` 打破并列；
- 趟次列表按业务日期倒序展示；
- 趟号按「车辆 + 当前账期」独立计算，按业务日期正序从 1 开始；
- 趟号是动态展示值，不存入数据。补录更早到家的旧账，会插入正确位置并使后续趟号顺延；
- 跨账期趟按到家日期归入账期。在途阶段显示的是预计趟号，收车后才确定。

对应实现：`src/domain.js` 中的 `tripDate`、`compareTripsAsc/Desc`、`tripSeq`。

## 3. 多车辆

- `vehicles` 保存车辆目录；车辆名称必填，车牌可选；
- 每条趟次和维修记录都必须有 `vehicleId`；
- 每辆车最多一趟在途，不同车辆可以同时在途；
- 车辆目录只统计自己的趟次、收入、支出和维修；
- 「汇总」目录聚合全部车辆，但各车趟号仍独立；
- 有历史记录的车辆采用停用，不物理删除；
- 在途车辆不能停用，且始终至少保留一辆启用车辆。

这些跨记录约束必须由 D1 原子触发器兜底，不能只依赖客户端提示或服务端“先查询、后写入”，否则两台设备交错保存会越过规则。

## 4. 金额与利润

- 单趟利润 = 该趟收入合计 − 该趟支出合计；
- 账期趟次利润只统计账期内已收车趟次；
- 维修保养按自身 `date` 和 `vehicleId` 统计，单独列账，不计入趟次利润；
- 净落袋 = 趟次利润 − 维修保养；
- 金额入口统一保留两位小数，不接受负数。

## 5. 严格在线与当前阶段边界

- Codex Sites D1 是业务唯一正式数据源；localStorage 只作为旧账迁移来源、一次性迁移标记、冲突导出副本和设备偏好；
- 未真实连接 API/服务器时，已加载账本只读。浏览、统计、详情和 JSON 导出可用；所有业务写入与 JSON 导入被拦截；
- 业务动作只有在服务器原子确认整批后才进入正式状态。失败提案不自动缓存上传，不显示成功；
- 每批使用全部 `expectedVersion` 和稳定 `operationId`；收车+收入、删除趟次+子账等必须全成或全不成；
- 网络恢复先重新 bootstrap 身份、fleet、权限和版本，再恢复写入；
- theme、当前车辆筛选、已查看报告属于设备偏好，可离线操作，不改变业务数据；
- 数据归属于 `fleet`，不直接归属于邮箱。内部角色预留 `owner` / `driver`，车辆分配使用 `vehicle_assignments`；
- 第一阶段只开放 owner 流程：owner 可查看和写入所属车队全部车辆；
- driver 只有活动车辆分配时才可读写该车，但司机邀请、登录和分配界面尚未开放；
- 登录使用 Sites 原生 Sign in with ChatGPT；手机号和短信身份留到第二阶段；
- 所有身份、车队归属、角色、车辆分配和业务输入检查在服务端完成；
- JSON 导入是经过摘要和覆盖确认的原子完整恢复，不改变登录身份；缺失或非法账期保留当前合法账期；
- 云端按独立记录和 version 保存，不允许回退为整份 JSON 覆盖或非原子客户端拆批；
- 离线写入、自动离线合并和逐条冲突解决不属于当前产品能力。
