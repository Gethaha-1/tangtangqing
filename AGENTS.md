# 项目协作规则

## 分支规则

- `develop` 是日常开发、测试和版本迭代的默认分支。
- 无论测试是否全部通过、版本是否稳定或是否已经发布，都不得自行将 `develop` 或任何其他分支合并到 `main`。
- 不得自行在 `main` 上直接提交、推送、变基、挑选提交或完成面向 `main` 的合并请求。
- 只有项目所有者在当前任务中明确要求“合并到 main”时，才可以执行涉及 `main` 的合并操作。发布 Netlify 或其他平台、测试通过、版本验收均不代表获得合并授权。
- 未收到明确合并指令时，所有后续修改、提交和推送都保留在 `develop` 或从 `develop` 派生的分支中。

## 本地工作目录与同步

- 优先使用已有且长期保留的本地 `develop` 工作目录继续迭代，不得为每次任务重新克隆完整仓库。
- 开始修改前仅获取 GitHub 的增量信息，并比较本地 `develop` 与 `origin/develop`；远端没有新提交时直接基于本地继续工作。
- 远端领先且本地没有冲突修改时才快进同步；本地领先时保留本地提交和未完成进度，不得为追平远端而重置或丢弃本地内容。
- 每次完成修改后可以提交到本地 `develop`，本地允许领先于 GitHub；不得把“完成修改”自动解释为获得远端推送授权。

## GitHub 与生产发布授权

- 版本更新完成后，可以不另行询问而保存并提交到本地 `develop`。
- 只有项目所有者在当前任务中明确授权推送后，才可以把本地提交推送到 GitHub `develop`；以往任务中的推送授权不自动延续。
- 推送 GitHub `develop` 不等于生产发布授权。即使代码已推送、测试全部通过、生产变量已配置或部署产物已生成，也不得自行发布到 Netlify 或其他生产环境。
- 只有项目所有者在 GitHub 推送授权之外再次明确允许生产发布后，才可以执行会更新生产别名、生产流量或生产运行版本的命令。
- 准备、预览、草稿部署和生产部署必须明确区分；没有额外生产授权时，任何命令都不得使用 `--prod`、切换生产别名或以其他方式让新版本接管生产流量。
