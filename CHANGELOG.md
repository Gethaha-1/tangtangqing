# 变更记录（CHANGELOG）

> 本文件按发生时间保留历史平台与当时的交付边界；旧条目中的 Sites、CloudBase、Cloudflare D1 或“尚未部署”只描述当时状态，不代表当前架构。当前线上架构以 README、ARCHITECTURE 和 `deploy/NETLIFY-SUPABASE.md` 为准。

## v1.8.0 · 去返程业务清单 · 2026-09-08 · 本地开发，未发布

- 一趟贯穿去返程；发车增加货物、货主多市场、快捷分组、逐人清单和整车原定运费。
- 去程运费按最多 3 位箱位比例与确定性分尾差分摊；协商抹零单独记录，不转成支出/收据/债务。
- 返程在同页增加货物、吨位×单价应收、可选实收、称重/掉称/协商扣款和可复用装卸地点。`null` 实收回退应收，数字 0 明确计零；增重必须人工确认。
- 发车/返程逐字段只写 scope 隔离的 IndexedDB 草稿，最终一次原子保存；失败和未知回执继续遵守第一轮恢复契约。
- JSON 升为 schema v4，新增 business 守恒，v1/v2/v3 仍可迁移；新增 SQLite `0004` 与独立 PostgreSQL marker 3 增量。
- 生产迁移、GitHub 推送、Netlify 部署和真实数据操作尚未授权、未执行。

## v1.7.0 · 第一轮恢复能力 · 2026-09-06 · 本地开发，未发布

- 快记清单、未加入清单的表达式、备注及油费字段按已核验账号/车队暂存 IndexedDB；刷新后可继续，跨标签冲突不静默覆盖。仍是非权威草稿，不会自动上传。
- 发送前持久化原请求；未知回执在重新登录/刷新后按原 ID 核对，成功回读后才清理。待核对任务不会阻塞已核验账本的只读显示。
- 大型 JSON 恢复改为校验分块暂存，最后一次原子提交；继续兼容 v1/v2/v3，正式 JSON schema 仍为 3。增加全账本版本守卫、任务取消、断点续传与到期暂存清理。
- 新增独立 PostgreSQL 增量 migration（内部 schema marker 2），不重建业务表；正式发布前须另行授权迁移及验收。
- 新增本机真实数据库及浏览器故障回归，Playwright 仅为开发依赖；补齐双平台维护和部署说明。Sites 保留 v1.6.1 归档，第二轮业务改造未实施。

## v1.6.1 · 旧版备份恢复超时修复 · 2026-09-05

- 将原子同步的关联记录校验合并到同一只读批次，消除费用/收入循环中的重复数据库往返；保留事务内权限、父记录、版本守卫和整批回滚。
- 保存请求等待完整回执上限调整为 45 秒，区分网络中断、服务器错误、超时和明确校验拒绝；未知回执保留只读及原操作供重连核对。
- 增加 v1/v2/v3 混合账目恢复、重复恢复、幂等和金额/fuel 守恒回归；旧版迁移规则保持兼容。
- 更新恢复与测试说明；不新增数据库迁移或依赖。

## v1.6.0 · 批量快记与现行架构清理 · 2026-09-05

### 批量快记

- 「记一笔」改为先加入当前页面的待保存清单，可连续添加普通费用和结构化油费，并在清单中修改、移除和核对合计；
- 新增顶部「清单 N」入口与底部安全区「保存全部 N 笔」主按钮，全部条目通过一次原子请求提交，完整回执后才清空并显示成功；
- 失败保留清单；未知回执使用稳定条目 ID 和原操作重连核对并锁定修改，未提交退出明确提醒，会话锁定清除内存草稿；
- 未新增数据库字段或线上 migration，生产数据继续使用现有 Supabase PostgreSQL schema。

### 现行架构清理

- 文档统一为当前 Netlify + Supabase 线上架构：Netlify 运行完整 Next.js，Supabase Auth + PostgreSQL 保存正式账本；
- 移除已退出运行路径的 Sites/CloudBase 身份入口、CloudBase 路由、容器/CFS 文件和部署说明；生产认证模式收敛为 Supabase，本地只保留 development + loopback 固定测试身份；
- SQLite、Drizzle 与 D1-shaped 类型明确为本地/兼容回归，线上 schema 以 `deploy/supabase/001_ledger.sql` 及后续 PostgreSQL migration 为准；
- 移除未使用的 Vinext/Vite 顶层依赖，并将 Next.js 16 已弃用的 `middleware.ts` 约定迁移到 `proxy.ts`；
- 更新部署标记、环境变量、测试、AI 接手和运维说明；迁移计划与验证报告保留为历史归档。

## v1.5.1-supabase.0 · 隔离验证分支

- 保留账本界面和严格在线 API；新增 Supabase 邮箱密码登录与 HttpOnly 会话。
- 新增 PostgreSQL 私有 schema、后端角色、参数适配与 SERIALIZABLE 原子批次。
- 新增真实 PostgreSQL、Supabase SDK 协议、生产 HTTP 冒烟测试及 Netlify 离线构建配置。
- 修复标准 Next.js 下 `/ledger` 未映射到账本 HTML 的入口问题；认证接口自行回写刷新 cookie，避免重复鉴权。
- 移除本分支的旧 Sites hosting 关联；原项目和未提交改动保持独立。
- 真实云端部署与国内移动网络验收需独立测试账号/资源，不以本地通过替代。
- 修复 Supabase 云端初始化脚本对项目管理员连接的误拦截；真实 Singapore 测试库、最小权限角色和双账号 Auth 验证通过。
- Supabase 云端数据库已强制 SSL；官方 CA、主机名和 `ttq_app` 重启后连接复测通过。

## 2026-08-09 · 当时未发布的本地改进（历史记录）

### 财务、fuel 与报告

- 首页主数统一为本账期净利润：账期趟次利润减同一车辆范围、同一账期内按发生日现金制计入的维修保养；单趟利润不分摊维修；
- 油费新增总价、单价、升数任意两项联算，金额/单价/升数分别使用 2/4/3 位精度，三项固定 1 分一致性校验；普通支出保持原金额盘；
- schema 升至 v3；`trip_expense.fuel` 在同步与 D1 使用定点整数保存，新增 `0002_lonely_shriek.sql`，旧油费 null/null 兼容且不伪造升数；
- 周期统计和报告统一总油费、结构化升数、加权均价、最低/最高、趋势和分车辆拆分，并明确旧油费覆盖缺口；
- 新增纯 `buildReportSummary()` 契约，月报与账期取交集、年报按完整自然年。没有增加报告 endpoint、调度、外部 API、视频能力或依赖。

### 收车与无障碍

- 首页和趟次详情改用全宽“确认收车”；结算后进入真实嵌套确认 sheet，展示车辆、日期、收入、支出、预计利润和零收入警告；
- 保存阶段锁定按钮并播报状态；只有完整服务器回执才关闭，网络/400/409/422/异常回执都保留确认层和表单；
- 最顶层 sheet 使用 dialog/aria-modal、inert 和 Tab 焦点圈，继续兼容系统返回、History 栈、共享元素转场及 reduced motion。

### 认证、安全与数据隔离

- 引入 provider-neutral `Principal` 与 edge adapter；公开请求身份头先剥离，再由 `sites` 或严格 loopback `local` 模式铸造内部身份；`cloudbase` 仅为 fail-closed 类型边界；
- bootstrap/sync/logout/本地登录统一同源 JSON POST，增加 Origin、fetch metadata、请求标记、2 MiB 流式 body 限制和全响应安全头；
- 本地测试入口使用固定纯数字登录名 `13800000000`；运行方式为 `TTQ_AUTH_MODE=local npm run dev`；
- 增加 fleet+membership scope 存储、多标签退出信号、BFCache 锁定和迟到回执 generation 防护；未归属旧 localStorage 不自动读取或迁移；
- schema v3 JSON envelope 增加 fuel/金额/数量守恒声明，并在导入前后和服务器回执后复核。
- 完整车队备份限定 owner 导出；driver 裁剪数据明确标记为不可完整恢复，并在迁移/差异规划前拒绝，防止误删未分配车辆账目。

### 当前边界

- 本批只在本地工作树实现和验证，未 commit、push、部署或修改任何线上资源；
- 当前仍是 Sites/Cloudflare Worker + D1 产物，不能直接部署为腾讯云标准 Node 应用；腾讯上海地域、个人主体封闭非经营试用、CloudBase Auth + MySQL 仅为下一批候选，尚未适配或认定合规。

## v1.5 · 本地共享元素转场实验 · 2026-08-08

### 弹层转场与交互

- 新增第一方 `src/ui-transition.js`，以 Web Animations API 实现 390ms Shared Element / Hero / Zoom 转场，不增加动画依赖；
- 趟次卡、更多菜单、车辆编辑、收入/支出/维修金额编辑、在途快记与收车入口从来源位置展开，关闭时反向回源；
- 来源被重绘、删除、隐藏或滚出视口时自动降级为轻缩放淡出；旧 Safari/微信缺少 `Element.animate` 时同步完成，不阻塞弹层；
- 支持 `prefers-reduced-motion`、在途动画取消、重复打开拦截、关闭后焦点恢复；手势关闭保留原有横滑/下拉动画，不重复播放回源；
- 保持 `sheetStack`、History API、安卓返回、`tapShield`、原子云写入与只读拦截边界不变。

### 依赖与本地边界

- 删除无源码/配置引用的直接 devDependency `@cloudflare/workers-types`；干净安装减少 1 个包、约 12 MiB，构建输出体积不变；
- 新增转场纯函数与控制器单测；构建前脚本继续只把第一方账本资源复制到 gitignored `public/ledger/`；
- 本实验仅在 `codex/local-shared-element-transition` 本地分支完成，未推送、未部署、未修改 Sites/Netlify/D1/认证。

## v1.5.0 · 2026-07-29

### 严格在线写入

- D1 成为唯一正式业务状态；已加载账本在无网络/API 不可达时切换只读，浏览、统计、详情和 JSON 导出仍可用；
- 所有业务入口统一为 clone proposal → server acknowledgement → commit UI，移除“先改全局 S + 150ms 后保存”的乐观模式；
- 保存中锁定相关提交；失败不显示成功、不改变正式列表、不写新的自动上传 localStorage 快照；
- 网络恢复先重新 bootstrap 身份、车队、权限和版本；`navigator.onLine` 不再直接解锁；
- 旧 `tangtangqing-cloud-cache-v1` 只检测一次：无差异清理、安全差异明确确认、双方变化停止并保留导出。

### 原子性、并发与幂等

- `/api/sync` 每个用户动作使用单个 D1 `batch()`，加入 transaction 内 membership、driver assignment 和逐记录 version guards；
- 新增 `operationId` + SHA-256 payload hash + `sync_commits` 回执，同请求超时重试安全回放，不同 payload 复用 ID 返回 409；
- 收车+多收入、删除趟次+子记录、车辆关联动作和 JSON 替换全成或全不成；
- 新增 `drizzle/0001_smart_the_twelve.sql` 和 runtime schema；
- 版本冲突、数据库约束和服务端拒绝均保证零部分写入。

### JSON、设备偏好与风险提示

- 保留完整 JSON 导出/恢复并在 Sites 可用；
- 导入前展示来源、车辆/趟次/收入/支出/维修数量和金额，明确“完整替换当前账本”；
- 缺失/非法/超长账期保留当前有效账期；相同文件重复导入幂等；服务器版本冲突不覆盖；
- 超过 500 operations 的导入明确拒绝，不恢复非原子 chunking；
- theme、当前车辆筛选、已查看报告改为设备偏好，不参与业务同步；
- 删除首页周期备份提醒和“每月导出 JSON”常规文案，保留真实服务器/冲突/未确认数据风险提示。

### 认证退出与交付

- 新增稳定 `/auth/logout`；生产委托 Sites dispatcher-owned `/signout-with-chatgpt`，localhost 只清除测试 cookie；
- UI 不再直连 provider，`return_to` 统一同源校验；应用不实现 Sites 三条保留认证路由；
- 账本“更多”页增加账号/退出区；退出前等待保存，未确认请求阻止退出；
- 登录首页保持手动“继续到账本”，不自动跳转；
- 更新架构、迁移、测试、AI 接手、依赖/体积和发布文档；未增加新依赖，未推 GitHub。

## v1.4.0 · 2026-07-28

### 第一阶段联网保存

- 项目安全适配为 Vinext/Next + Codex Sites Worker，保留原账本 UI、交互和 `src/domain.js` 业务口径；
- `.openai/hosting.json` 声明 D1 逻辑 binding `DB`，R2 保持未启用；
- D1 成为正式数据源；localStorage 仅保留旧账迁移来源、设备缓存和设备偏好；
- schema 新增内部 `users`、`identities`、`fleets`、`fleet_members`、`vehicle_assignments`，角色预留 `owner` / `driver`；
- 业务数据按车队拆成车辆、科目、趟次、收入、支出、维修等独立记录，金额以整数分保存；
- 每条记录使用 `updated_at`、`version` 和 `expectedVersion` 做乐观并发控制，不再整份 JSON 覆盖；
- 正式初始 migration 与运行时 schema 对齐 `CHECK`、外键、索引和原子触发器，封住最后启用车辆、发车/停用及删除趟次/并发新增子账目的竞态；
- 服务端只信任 Sites 转发身份头，并在每次读写中检查内部 user、活动车队 membership、角色和车辆 assignment；
- 第一阶段自动建立 owner 车队，owner 可查看和写入车队全部车辆；司机完整接入流程暂未开放。

### 登录与本地验证

- 新增面向司机的移动端登录页，支持日间「宣纸账房」和夜间「瓷青泥金」系统主题；
- 正式登录使用 Sites 原生 Sign in with ChatGPT；邮箱只作为当前 identity 的 provider subject，不作为业务主键；
- 新增仅限 localhost 的固定 owner 测试替身，使用 HttpOnly cookie，便于本地验证登录、退出和权限保护；
- `/ledger` 与账本静态资源要求可信身份，未登录请求会被拦截。

### 迁移、断网与测试

- 检测原 `tangtangqing-data` schema v2：云端为空时明确提示上传；双方都有数据时绝不静默覆盖；无旧账时读取或初始化云端；
- schema v2 可拆分为独立云记录并无损重组；首次上传可重复执行，上传后核对记录数量和收入/支出/维修金额守恒；
- 大型旧账按 200 条分批续传；设备待上传记录只有在云端 version 未变化时自动补传，冲突时保留设备备份并停止覆盖；
- 正常保存只推进本批基线；恢复联网后以远端为底做三方合并，其他设备新增的不同记录不会被误生成删除；
- 继续支持 JSON 导出与明确恢复；旧 localStorage 迁移后不自动删除；
- 断网或保存失败明确标记「尚未上传」；设备缓存失败时要求立即导出，缓存不冒充云端成功；
- 恢复备份中的文本、属性和科目图标使用分上下文安全渲染，补充恶意 schema v2 持久化 XSS 回归；
- 新增云同步与服务端契约测试，覆盖授权边界、归属字段拒绝、版本冲突、迁移守恒、正式 migration、原子业务约束及重复/孤儿记录保护；
- 保存 D1 schema 与 Drizzle migrations，并补充本地启动、认证替身和迁移冒烟文档。

### 交付边界

- 本轮仅供本地测试：没有创建 Sites 项目，没有部署生产，没有推送 GitHub，也没有创建 PR；
- 司机邀请/分配 UI、手机号/短信身份、更完整离线队列和生产运维留到第二阶段。

## v1.3.0 · 2026-07-28

### 趟次与账期

- 趟次列表和动态趟号统一按到家日期计算，补录旧账会插入正确位置并使同车后续趟号顺延；
- 新增自定义账期，开始、结束日期由用户设置，首页、趟次、统计、维修和账期报告统一使用该范围；
- 跨自然年趟按到家日期归入账期，在途趟显示预计趟号。

### 多车辆

- 新增车辆管理、车辆目录和全部车辆汇总目录；
- 趟次、维修均增加 `vehicleId`，每辆车独立编号、独立统计；
- 每辆车最多一趟在途，不同车辆可以同时在途；
- 车辆支持名称、车牌、停用和重新启用；在途车不能停用。

### 数据迁移与工程化

- schema 升至 v2；v1 备份自动建立「原有车辆」，旧趟次和维修无损归入；
- 业务规则与迁移拆到 `src/domain.js`，可同时在浏览器和 Node 中运行；
- 新增 `tests/domain.test.js`、真实备份守恒校验脚本、`BUSINESS-RULES.md` 和 `DATA-MIGRATION.md`；
- 内置自检从 21 项扩充到 28 项；
- 已用 2026-07-28 真实备份验证：2 趟、5 笔维修及收入/支出/维修总额迁移前后一致；
- 登录、云存储、短信和跨设备实时同步明确暂缓。

## v1.2.0 · 2026-07-14

### 视觉（全新双主题）
- 日间主题重做为「宣纸账房」：宣纸底、朱砂主色、文书双线框主卡、区块朱记、宋体展示数字（双主题统一）；
- 夜间主题重做为「瓷青泥金」：瓷青纸底、泥金主色，替代旧"夜路仪表盘"（同宗纸墨意象 + 夜路低眩光）；
- 收讫章升级双圈样式，双主题常显；移除夜间 LED 边条与仪表刻度贴片；
- 底部导航、主题按钮、当时的收车控件箭头全部由 emoji 换为线性 SVG 图标（科目 emoji 属用户内容，保留）；
- 背景氛围渐变（画在 body::before 固定层，规避 iOS background-attachment 问题）。

### 交互
- **系统返回键接管**：安卓返回键/手势现在逐层关闭弹层，而不是退出页面（History API + expectPop 计数，详见 ARCHITECTURE §6）；
- 快记支持**改日期**（默认今天，折叠控件，一次点开）；
- 改一笔支出时可**顺手改科目**（金额盘顶部科目 chips）；
- 收车时**零收入确认**：没填收入会先问一句，防止忘填；
- 全局**防连点**（600ms 提交锁）与**防穿透**（关层 350ms 触摸挡板）；
- 键盘对尾随 `+`（如 `300+`）容错，不再误报"先输入金额"。

### 数据安全
- 保存失败即时 toast 警告并引导导出备份；
- 首页新增**备份提醒横幅**：从未备份或超 30 天未备份时出现，点击直接导出；导出成功记录 `settings.lastBackupAt`；
- `migrate()` 增加导入数据清洗：金额强转数字（坏值归 0）、缺失数组补齐——恢复来历不明的备份文件也不会污染统计。

### 修复
- 收车结算页：有金额的**停用收入科目**现在会显示（此前金额计入利润却不可见，与详情页口径不一致）；
- 首页仪表盘：支出占比 >100% 时文字显示真实百分比（此前被截在 100% 产生误导），进度条最多画满；
- 首页脚注版本号错标 v1.0 → 版本号统一由 `APP_VER` 常量渲染（单一来源）；
- `openAmtPad` 的 `okLabel` 配置此前从未生效 → 键盘工厂新增 `setOk()`，按钮文案随场景变化（✓ 保 存 / ✓ 记 上 / ✓ 填 好）；
- `fmtMD` 中的无效 `replace('-','-')` 空操作清理；
- 原 `closeTopSheet` 死代码被历史栈机制正式启用。

### 工程化
- 新增 **[JS-6] 内置自检**：21 项断言覆盖统计口径、迁移清洗、金额计算与转义；地址加 `#test` 或控制台 `runSelfTests()` 运行，结果弹窗展示（复用报告弹层）；口径测试用夹具数据、测后还原；
- 建立完整文档体系：README / ARCHITECTURE / DESIGN / TESTING / AI-GUIDE / CHANGELOG / BACKLOG；
- 应用文件更名为 `index.html`（利于 git 固定文件名与 GitHub Pages 直接托管）。

## v1.1.0

- 文件头部加入段落结构索引注释（[CSS-1]~[JS-5]）；
- schema 迁移框架（SCHEMA_VER + migrations 表）；
- 存储三级降级（localStorage → window.storage → 内存）与预览模式横幅。

## v1.0.0

- 首个可用版本：发车/快记/收车/趟次详情/补录、双主题、科目管理（软删除）、月报年报生成与复制分享、备份导出恢复、月度柱图与年度统计。
