# 数据迁移（DATA-MIGRATION）

适用版本：**v1.4.0**。页面业务快照仍为 `schemaVersion = 2`；v1.4.0 新增的是从本机 schema v2 到 D1 独立记录的首次联网迁移。

## 1. v1 → v2 规则不变

历史 v1 备份仍先统一经过 `TTQDomain.migrate()`：

1. 自动创建 `{ id: "vehicle_legacy", name: "原有车辆" }`；
2. 所有旧趟次和维修记录归入这辆车；
3. 旧金额、日期、科目、备注、ID 和创建时间不改写；
4. 默认账期为迁移当年的 1 月 1 日至 12 月 31 日；
5. 缺失数组补齐，金额仅做数字类型清洗；
6. 导入、启动和云端上传前都必须经过同一迁移入口。

## 2. schema v2 → D1 独立记录

首次上传不会把整个 JSON 存成一个云端字段。适配层会拆成：

- `fleet_settings`
- `category`
- `vehicle`
- `trip`
- `trip_expense`
- `trip_income`
- `maintenance`

每条记录都有稳定 id、排序位置和独立 version。收入、支出、维修金额由服务端换算成整数分保存。上传顺序先满足车辆→趟次→子账目的外键依赖，删除时反向处理。

## 3. 首次打开的三个分支

系统检测旧 `localStorage["tangtangqing-data"]`，同时从 `/api/bootstrap` 读取车队是否已初始化。

| 状态 | 行为 |
|---|---|
| 本机有旧账，云端为空 | 显示车辆、趟次、收入、支出和维修摘要；用户明确选择「核对并上传」或「新建空白云端账本」 |
| 本机和云端都有业务数据 | 绝不自动合并、上传或覆盖；建议先导出本机 JSON，再明确进入云端账本 |
| 本机没有旧账 | 云端有数据则使用云端；两边都空则创建并保存默认新账本 |

“本机有快照但没有业务记录”也会按明确选择处理，避免把旧设置静默盖到云端。

## 4. 可重复执行与守恒

迁移必须满足：

- 以最近 bootstrap records 为基线，只提交差异；
- 大型账本按最多 200 条记录分批；中断后保持“未初始化”，下次从已写入记录继续；
- 新记录使用 `expectedVersion = 0`，已存在记录必须匹配当前 version；
- 部分记录已成功时，刷新基线后重试只补剩余差异；
- 重试以完整远端为底做三方合并，保留其他设备新增的不同记录，不会把它们生成成 `delete`；
- 重复 id、孤儿收入/支出或版本冲突会停止静默重组；
- 上传后重新从 D1 读取并重组 schema v2；
- 迁移前后按本次目标 key 核对车辆、科目、趟次、收入、支出、维修数量，以及收入、支出、维修总额；远端同期新增的不同 key 会保留并进入最终状态；
- 任一项不守恒，不显示“已保存到云端”。

旧 localStorage 不会因上传成功被自动删除，便于用户复核和回退。

## 5. JSON 导出与恢复

- 「备份数据」继续导出完整 schema v2 JSON。
- 「恢复数据」先经过 `TTQDomain.migrate()` 清洗，再明确询问是否替换当前账本。
- 用户确认后，适配层把恢复结果与当前云端基线比较，生成逐记录新增、更新和删除。
- 只有服务端保存并回读守恒后才显示云端成功；失败内容只标记为设备暂存。
- 真实用户备份只作为本地输入，不复制进仓库、不写回原文件、不上传到第三方。

当本机和云端都有数据且需要人工合并时，先分别导出备份；第一阶段不提供自动双向合并。

## 6. 验证方式

所有自动化测试：

```bash
npm test
```

真实备份 v1 → v2 守恒：

```bash
node scripts/verify-backup.js /绝对路径/趟趟清备份.json
```

手工首次迁移与冲突验证见 [TESTING.md](TESTING.md)。
