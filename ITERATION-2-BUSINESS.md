# 第二轮去返程业务交付说明

适用代码版本：v1.9.0。本文记录已实施范围、生产迁移状态与验收边界。

## 已实施

- 一条趟次贯穿发车、去程、返程和收车，不拆成两趟。
- 发车页可选拉菜/普货/其他，用货主分组批量加入本趟，或每次只加一个货主市场。分组主货主只是快捷入口；本趟移除任意圆圈只改清单快照。
- 整车运费按最多 3 位小数的箱位比例分摊，使用确定性最大余数法分配到分的尾差。每位货主的协商抹零单独保存，最终额为分摊额减抹零。
- 返程页同时处理货物、装车吨位、单价、应收、可选实收、卸车吨位、掉称参考/确认、协商扣款和两端地点。实收 `null` 回退应收，实收 `0` 明确计为零。
- 卸车吨位高于装车吨位时客户端需人工勾选，服务端再次强制。系统不猜测掉称单价，不把扣款再从已确认营收减一次。
- 装卸市、区县分栏，厂家/地点、道路、装卸和其他提醒均可选且可重用。定位按钮按次告知并征得同意，再使用 BigDataCloud 免密钥客户端查询；无持续定位、付费地图或外部 AI。旧地点原文和新市县均能同步、备份恢复。
- 发车、返程和快记都遵守本机草稿、fleet+membership 隔离、未知回执核对和最终一次原子保存边界。
- JSON 升为 schema v4；保留 v1/v2/v3 导入，v4 envelope 新增业务数量和 fingerprint 守恒。
- 空白或改回原样的表单直接返回；底部“放弃修改”排空本机草稿写入后清理，不上传、不删除已有云端清单。
- 发车完整清单、返程已加入快照和快记清单返回时，先落可靠请求日志即关闭，后台用原有原子请求上传；回执未知保留同一日志，首页可核对，不能重复入账。未加入的额外输入仅留本机。
- 去返程金额、吨位、箱位、抹零和掉称均使用网页大键盘；保留最多 3 位吨位/箱位/掉称精度、两位金额、实收空与 0 的区别。

## 数据与口径

`fleet_settings.business_json` 保存可复用目录，`trips.business_json` 保存趟次快照。两者都是顶层 JSON 对象，服务端限 1 MB 并强制字段、引用、小数精度和公式。

新去程清单存在时，营收取 `outbound.finalTotal`；新返程清单存在时，营收取 `returnTrip.effectiveAmount`。为旧界面与备份连续性保留的 `cargo`/`back` 收入行在这两种情况下不再累加，防止重复计收入。

## 已覆盖的自动化

- 去程 6000 元按 1/2/3 箱位精确分摊，100 元三等分的 33.34/33.33/33.33 尾差，抹零合计和最终额。
- 返程 30 吨 × 240 元 = 7200 元，实收空/0、修改后重算、3 位吨位/2 位金额限制。
- 掉称参考、增重确认、业务营收不重复、设置引用、云记录往返与 v1/v2/v3 默认迁移。
- SQLite 增量列默认值与 JSON 对象约束，PostgreSQL 服务端契约与持久化接线。
- 390×844 真实浏览器完成发车分摊、返程 `30×240`、明确实收 `0`、原子保存和服务器回读。

## 生产状态与验收边界

- 2026-09-08 项目所有者确认生产迁移后，已执行 recovery 与 `20260908120000_trip_business.sql` 增量；数据库 marker `[1,2,3]`、两处业务 JSON 列、恢复表和 7 个版本触发器已验证。未回填或删除真实账目。旧版文档“尚未授权迁移”已过时。
- 本次 v1.9.0 不再执行数据库迁移、不变更账号验证或数据库权限。代码仅提交/推送 `develop`，生产仍用原 Netlify 站点；不合并 `main`。
- 2026-09-09 v1.9.0 已完成 GitHub develop 推送及 Netlify 生产部署，线上只读冒烟通过；具体代码提交、部署编号和验证边界见 [生产发布记录](deploy/NETLIFY-SUPABASE.md)。
- 隔离浏览器覆盖慢回执先关闭、空白退出、回改无脏状态、放弃不上传、回执丢失不重复、未加入输入保留、市县回读、大键盘与定位晚到保护；第三方 API 使用协议夹具，不把模拟定位当作国内真实移动网络验收。
- 手机仍需验证位置权限和 BigDataCloud 可达性；免密钥服务不可用/地址不全时明确提示并保留手填，不保证每个坐标都有完整区县。

### 地址服务依据

- [BigDataCloud 客户端逆地理编码 API](https://www.bigdatacloud.com/geocoding-apis/free-reverse-geocode-to-city-api)：只允许客户端本次实时设备坐标，`zh` 查询与行政区划字段。
- [免费模式与隐私说明](https://www.bigdatacloud.com/docs/article/why-is-reverse-geocoding-api-free)：不需要 API key，匿名 GPS/IP 信号用于改善定位数据；页面同意提示已说明。
